@model IEnumerable<PayDaySample01.Domain.Models.Relation>

@{
    ViewBag.Title = "Index";
}

<h2>Relaciones de Empleados con Dependientes</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RelationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RelationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RelationId })
        </td>
    </tr>
}

</table>
